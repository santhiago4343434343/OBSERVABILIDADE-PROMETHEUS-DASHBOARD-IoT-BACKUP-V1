# Angular 18 declara um peer opcional de tailwind v2/v3; usamos o v4 via
# @tailwindcss/postcss (.postcssrc.json). Este flag evita o erro ERESOLVE
# no npm install — tanto local quanto no build do Docker.
legacy-peer-deps=true
